<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Title</title>
</head>
<body>
    <h1>Welcome<% if (currentUser) {%>,
        <%= currentUser.name %>!
    <% } %>
    </h1>

    <% if (message) { %>
        <div><%= message %></div>
    <% } %>

    <a href="signup">Sign Up</a><br>
    <a href="login">Log In</a>
</body>
</html>